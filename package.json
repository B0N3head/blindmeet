{
  "name": "blindmeet",
  "version": "1.0.0",
  "description": "Private availability scheduler — only the organizer sees the full picture",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.1",
    "express": "^4.18.2"
  }
}
